services:
  - type: web
    name: midi-piano-trainer-backend
    runtime: docker
    plan: free
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: MAX_UPLOAD_MB
        value: 25
